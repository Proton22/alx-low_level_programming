#include "main.h"
/**
 * mul - print multiplication of two integers
 * @a: the first integer
 * @b: the second integer
 * Return: the multiply of two
 */
int mul(int a, int b)
{
int c;
c = a * b;
return (c);
}
