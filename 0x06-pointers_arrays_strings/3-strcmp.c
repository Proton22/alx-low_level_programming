#include "main.h"
#include <string.h>
/**
 * _strcmp - print the character
 * @s1: a character
 * @s2: a character
 * Return: if executed properly
 */
int _strcmp(char *s1, char *s2)
{
strcmp(s1, s2);
return (s1);
}
