#include "main.h"
#include <string.h>
/**
 * _strlen - print the length
 * @s: a character argument
 * Return: if executed properly
 */
int _strlen(char *s)
{
int l;
s = "Pointless!";
l = strlen(s);
_putchar(l);
return (l);
}
