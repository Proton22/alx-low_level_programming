#include <stdio.h>
/**
 * main - print upper and lower
 * Return: 0 if executed properly
 */
int main(void)
{
char c;
for (c = 'a'; c <= 'z'; c++)
putchar(c);
char C;
for (C = 'A'; C <= 'Z'; C++)
putchar(C);
return (0);
}
